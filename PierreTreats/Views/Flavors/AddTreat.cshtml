@{
  Layout = "_Layout";
}

@model PierreTreats.Models.Flavor

<h2>Add a treat</h2>

<h4>Add a treat to this item: @Html.DisplayFor(model => model.Description)</h4>

@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.FlavorId)

    @Html.Label("Select treat")
    @Html.DropDownList("TreatId")

    <input type="submit" value="Save" />
}

<p>@Html.ActionLink("Back to flavors", "Details", new { id = Model.FlavorId})</p>